#ifndef GAME_ANIMATION_SCRIPTS_H
#define GAME_ANIMATION_SCRIPTS_H

#include "ECS/Entity.h"

void coin_animation(Entity *ent);
void enemy_animation(Entity *ent);
void player_animation(Entity *ent);

#endif
